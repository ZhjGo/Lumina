# LUMINA Chrome 扩展

这是 LUMINA 灵感起始页的 Chrome 扩展版本，提供与 Next.js 网站完全一致的沉浸式体验。

## 快速开始

### 开发模式

```bash
npm install
npm run dev
```

### 构建扩展

```bash
npm run build
```

构建输出在 `dist/` 目录。

### 安装到 Chrome

1. 打开 Chrome 访问 `chrome://extensions/`
2. 启用"开发者模式"
3. 点击"加载已解压的扩展程序"
4. 选择 `dist/` 目录

## 技术栈

- React 19
- Tailwind CSS 4
- Framer Motion
- Vite + CRXJS
- TypeScript

## 项目结构

```
src/
├── components/      # React 组件
│   ├── features/   # 业务组件
│   └── ui/         # 基础 UI 组件
├── data/           # 数据文件
│   ├── links.ts
│   └── wallpapers.ts
├── lib/            # 工具函数
├── newtab.html     # 新标签页 HTML
├── newtab.tsx      # React 入口
└── index.css       # 全局样式
```

## 功能特性

- 🌊 沉浸式壁纸背景系统
- 🕐 实时数字时钟
- 🔍 智能搜索栏（百度/搜狗/必应）
- 📚 分类导航网格
- 🎨 Glassmorphism 磨砂玻璃效果
- ✨ Framer Motion 流畅动画
- 📱 完全响应式设计

## 代码复用

本项目从 Next.js LUMINA 项目复用了 90%+ 的代码，包括：
- 所有 React 组件
- Tailwind CSS 配置和样式
- 数据文件和工具函数
- 动画效果和交互逻辑
